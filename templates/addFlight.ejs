<!DOCTYPE html>
<html lang="en">

<head>
    <title>Add Flight - Watchlist</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <header>
        <h1>Add Flight to Watchlist</h1>
    </header>

    <main>
        <h2>Flight Information</h2>
        <form action="/saved/add" method="POST">
            <label for="callsign">Callsign / Flight Number *</label>
            <input type="text" name="callsign" id="callsign" required placeholder="e.g., DAL123, AA456"
                value="<%= prefill.callsign || '' %>">

            <label for="flightIata">Flight IATA Code</label>
            <input type="text" name="flightIata" id="flightIata" placeholder="e.g., DL123"
                value="<%= prefill.flightIata || '' %>">

            <label for="airline">Airline</label>
            <input type="text" name="airline" id="airline" placeholder="e.g., Delta Air Lines"
                value="<%= prefill.airline || '' %>">

            <h2>Route</h2>
            <label for="departureAirport">Departure Airport</label>
            <input type="text" name="departureAirport" id="departureAirport"
                placeholder="e.g., Los Angeles International" value="<%= prefill.departureAirport || '' %>">

            <label for="departureIata">Departure IATA</label>
            <input type="text" name="departureIata" id="departureIata" placeholder="e.g., LAX" maxlength="4"
                value="<%= prefill.departureIata || '' %>">

            <label for="arrivalAirport">Arrival Airport</label>
            <input type="text" name="arrivalAirport" id="arrivalAirport"
                placeholder="e.g., John F Kennedy International" value="<%= prefill.arrivalAirport || '' %>">

            <label for="arrivalIata">Arrival IATA</label>
            <input type="text" name="arrivalIata" id="arrivalIata" placeholder="e.g., JFK" maxlength="4"
                value="<%= prefill.arrivalIata || '' %>">

            <h2>Details</h2>
            <label for="status">Status</label>
            <select name="status" id="status">
                <option value="">Unknown</option>
                <option value="scheduled">Scheduled</option>
                <option value="active">Active</option>
                <option value="landed">Landed</option>
                <option value="cancelled">Cancelled</option>
                <option value="delayed">Delayed</option>
            </select>

            <label for="priority">Priority</label>
            <select name="priority" id="priority">
                <option value="low">Low</option>
                <option value="medium">Medium</option>
                <option value="high">High</option>
            </select>

            <label for="notes">Notes</label>
            <textarea name="notes" id="notes"
                placeholder="Add any notes about this flight..."><%= prefill.notes || '' %></textarea>

            <button type="submit">Save Flight</button>
            <a href="/saved">Cancel</a>
        </form>
    </main>

    <footer>
        <a href="/">Home</a> | <a href="/saved">Back to Watchlist</a>
    </footer>
</body>

</html>